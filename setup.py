from setuptools import setup, find_packages
setup(name="deepplanner", version="0.1.0", description="Agent Planning Harness with hierarchical task decomposition",
      packages=find_packages(), python_requires=">=3.9")
